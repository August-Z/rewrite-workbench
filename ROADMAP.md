# Roadmap

更新：2026-09-09。当前处于 M0；本文描述计划，完成状态以 [status](docs/status.md)、GitHub issue 和可复核产物为准。

时间是以一名熟悉 Rust 与前端的主要开发者、可持续投入为前提的相对估算。Agent 可协助实现与审查，但不会消除兼容性验证、人工评测和分发成本。里程碑按验收完成，不按日期强行发布。

## 总体安排

| 阶段 | 参考投入 | 主要结果 | 出口条件 |
| --- | --- | --- | --- |
| M0 · Project bootstrap | 1–2 天 | 计划、架构、任务、GitHub 仓库、Codex 接入 | 新对话可从仓库定位当前状态和下一项任务 |
| M1 · Correctness foundation | 第 1–2 周 | 锁定依赖、fixture、绑定识别、两种操作、补丁核心 | 正反例与 Unicode/冲突测试通过；有可运行 CLI 垂直路径 |
| M2 · Local alpha | 第 3–4 周 | 本地服务、可视化条件、候选原因、diff、patch、recipe | 支持范围内端到端验收通过；如实记录外部可用性证据状态 |
| M3 · Public v0.1 | 第 5–6 周起 | 带保护的应用、安装包、真实在线样例、发布资料 | 分发与数据写入验证完成；事实性说明支持范围；明确发布授权 |
| M4 · Evidence-led expansion | v0.1 后 | 根据真实场景扩展操作与接入 | 有复现案例与传播/使用证据，能解释为什么值得增加维护成本 |

两周目标是可验证的垂直实验，不是完整公开产品承诺。竞品基线和目标用户评测并行开展；暂时约不到试用者不阻止正确性底座和本地 alpha 的实现，但不能将未开展的评测写成通过。

## M0 · Project bootstrap

- 产品与技术决策落到便携文档。
- 创建仓库、清晰 README、MIT 许可证、贡献与 issue 模板。
- 用 GitHub milestones/issues 跟踪实施任务；核心方案保留在 Git 文档中。
- 添加 Codex 本地项目；`AGENTS.md` 负责引导新任务读取状态与计划。
- 留出下一项明确任务 RWB-001，而不是生成大量无人负责的同时运行任务。

完成证据：远程 main 与本地 commit 一致；项目在 Codex 项目列表中可见。若另建验证任务，必须确认它读取了正确仓库与状态。

## M1 · Correctness foundation

任务：RWB-001、002、003、004。

顺序：工作区/fixture → binding → 修改计划 → 两操作。模块契约稳定后，fixtures、patch 测试可有限并行。

首版支持：TSX、直接命名导入（含本地别名）、作用域遮蔽、显式 JSX 属性改名、字面字符串值映射。

验收：

- 提交锁文件与明确工具链；实际 setup/test 命令已运行并记录。
- 所有标注为禁止修改的 fixture 不生成 edit；不确定与冲突有原因。
- 导出补丁对应固定输入；保留无关字节；重跑满足对应规则的幂等约束。
- 覆盖 Unicode、CRLF、BOM、重复属性、spread、parse error、重叠 edit。
- 一个 CLI 垂直路径可以读取合成项目、生成候选与补丁；此阶段不写目标文件。

## M2 · Local alpha

任务：RWB-005、006、007。

范围：本地目录 → 选择节点/两种预设操作 → 预填并确认导入与属性条件 → ready/not_matched/skipped/conflicted/invalid → diff → 勾选 → patch → 保存 recipe 与 fixtures。Alpha 不识别任意 before/after，也不从正反例自动归纳条件。

验收：

- UI 和 CLI 使用同一 Rust core、相同 recipe 与输入快照；相同选择导出相同 patch。
- 用户改变条件后，旧请求结果不能覆盖新结果；修改后的文件不继续使用旧预览。
- 本地 HTTP 只服务显式项目范围，有基本来源与会话约束，不执行目标项目脚本。
- 支持 loading、empty、parse error、cancelled、stale snapshot、unsupported 等真实状态。
- 完成至少一个真实任务的端到端验证；记录竞品基线与用户试用是否实际开展。

候选体验门槛：6 位目标用户中至少 4 位独立完成；与最佳对照相比正确任务的中位耗时降低 30%；禁止修改样例零误改。这是项目决策门槛，不是行业标准，不替代 Star 观察。若样本不足或无优势，记录原因，优先改善操作，不自动扩张范围。

## M3 · Public v0.1

任务：RWB-008、009、010、011。

验收：

- 直接应用有单独审查：文件 hash/原文校验、冲突拒绝、明确失败结果；不宣称跨文件原子事务。
- 安装包有校验和、版本与支持平台声明；先覆盖 macOS arm64 和 Linux x64，其他平台待实际验证后列为支持。
- 在线体验采用同 core 或明确可验证的同等语义，不以固定返回值冒充真实引擎；仅公开合成样例，用户代码默认不上传。
- README 的命令都能执行；不存在尚未发布包的安装示例。
- 发布前完成对应许可证/依赖 notice、功能边界、已知限制与 demo 校验。
- 记录落地页触达、GitHub 跳转、同期净增 Star、自发分享和后续场景；不购买、交换或自动刷 Star。

公开仓库的创建不代表已经发布 v0.1。版本发布、包发布与宣传作为独立动作执行。

## M4 · Evidence-led expansion

任务：RWB-012。按收到的真实任务选择，不同时开展全部方向：

1. 对命名导入 API 的迁移，优先保留本地 binding。
2. 受限函数参数/选项改写，需要正确处理求值顺序、副作用和可选调用。
3. 正反例辅助条件收敛；仍给用户显示最终条件。
4. 仅当任意模式确有需求时引入 ast-grep；处理双引擎位置和语义边界。
5. re-export、路径别名和工作区解析；独立定义覆盖与未解析行为。
6. 编辑器入口、可复用规则包、CI dry-run；更晚再评估 MCP/AI 配置生成。

新增语言、桌面壳、云端仓库处理、完整迁移平台不属于默认承诺。

## 调整规则

- 正确性不过关：停止增加操作类型，先收窄支持范围并补回归。
- 操作体验不优于已有工具：改善入口/条件表达，或转为上游功能；不靠功能数量回避结论。
- 有目标曝光但 Star 与自发分享持续很弱：调整定位和案例，重新评估独立项目投入。
- 缺少曝光：补一次有记录的分发实验，不据此断言需求不存在。
- 任何调整都更新对应决策、backlog 和 status；不把新想法直接加入正在执行任务。

详细任务见 [backlog](docs/backlog.md)，增长验证见 [launch validation](docs/research/launch-validation.md)。
